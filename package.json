{
  "name": "fp-ts-lambda-routing",
  "version": "1.0.0",
  "description": "Library that helps with routing lambda events to the right function, wrapped in an fp-ts monad",
  "keywords": [
    "fp-ts",
    "routing",
    "functional"
  ],
  "homepage": "https://github.com/TODO/package-name",
  "repository": {
    "type": "git",
    "url": "https://github.com/TODO/package-name.git"
  },
  "bugs": {
    "url": "https://github.com/TODO/package-name/issues"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "test": "jest",
    "build": "tsc",
    "publish": "npm run test && npm run build && npm publish --access public && npm version patch"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "author": "Sam Van Overmeire",
  "license": "MIT",
  "dependencies": {
    "@types/aws-lambda": "^8.10.81",
    "fp-ts": "^2.11.1"
  },
  "devDependencies": {
    "@types/jest": "26.0.15",
    "jest": "^26.0.0",
    "ts-jest": "^26.0.0",
    "typescript": "^4.3.5"
  }
}
